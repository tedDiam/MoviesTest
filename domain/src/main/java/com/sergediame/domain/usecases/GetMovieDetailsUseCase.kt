package com.sergediame.domain.usecases

import com.sergediame.domain.Repository
import com.sergediame.domain.entities.MovieDetails
import javax.inject.Inject

/*
class GetMovieDetailsUseCase @Inject constructor(
    private val repository: Repository
) : BaseUseCase<Int, MovieDetails>() {
    override suspend fun doJob(params: Int): MovieDetails = repository.getMovieDetails(params)
}*/
